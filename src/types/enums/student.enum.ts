export enum StudentCountry {
  TH = 'th',
  VN = 'vn',
}

export enum StudentLanguage {
  TH = 'th',
  VN = 'vn',
  EN = 'en',
}

export enum StudentPersonalTitle {
  MR = 'Mr.',
  MRS = 'Mrs.',
  MS = 'Ms.',
  DR = 'Dr.',
  Prof = 'Prof.',
}

export enum StudentGender {
  FEMALE = 'female',
  MALE = 'male',
}
